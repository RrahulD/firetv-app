<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.AppCompatButton xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/seasonButton"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/_12dp"
    android:background="@drawable/primary_button_background"
    android:focusable="true"
    tools:text="Season 1"
    android:nextFocusLeft="@+id/seasonButton"
    android:nextFocusRight="@+id/seasonDetailsRecyclerView"
    android:gravity="center"
    android:focusableInTouchMode="true"
    android:fontFamily="@font/montserrat_bold"
    android:paddingLeft="@dimen/_25dp"
    android:paddingRight="@dimen/_25dp"
    android:paddingTop="@dimen/_12dp"
    android:paddingBottom="@dimen/_12dp"
    android:textColor="@drawable/text_button_color"
    android:textSize="@dimen/_15sp" />
